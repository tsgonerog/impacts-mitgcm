#-- Packages compiled into the DINO_1deg adjoint (Tapenade); see README.md.

#gfd
#-mom_fluxform
exch2
mom_vecinv
diagnostics
generic_advdiff
monitor
#mnc

#cal
#exf
gmredi
# kpp: not compiled into the adjoint since 2026-09-12, nor into the forward model
# (code/packages.conf, which says how to turn it back on). useKPP is .FALSE. in every
# adjoint namelist; while compiled, its four 3-D arrays were carried in every binomial
# snapshot and in Tapenade's aliasing warnings. input_tap/data.kpp was deleted the
# same day. For an adjoint with KPP, also decide useKPPinAdMode (data.autodiff); the
# -nocheckpoint list in tap_nocheckpoint.txt was profiled without KPP.
#kpp
#seaice
#salt_plume
#obcs
#down_slope

#debug
#autodiff
#cost
#ctrl
#grdchk
#ecco
#smooth
#profiles
rw
mdsio
# cd_code: the C-D scheme for the Coriolis terms. Never compiled into the adjoint, and
# not into the forward model since 2026-09-12 (code/packages.conf, which says how to
# turn it back on). An adjoint with the scheme would also need useCDscheme=.TRUE. and
# tauCD in input_tap/data and, when it starts from a pickup, one written with the
# scheme on: a restart reads pickup_cd.<iter> (pkg/cd_code/cd_code_ini_vars.F).
#cd_code
tapenade
adjoint # this pkg_group will include the packages "autodiff, ctrl, cost, grdchk"
