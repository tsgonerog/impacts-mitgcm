#-- Packages compiled into the DINO_1deg forward model; see README.md.

#gfd
#-mom_fluxform
exch2
mom_vecinv
diagnostics
generic_advdiff
monitor
#mnc

#cal
#exf
gmredi
# kpp: not compiled since 2026-09-12, here or in the adjoint (code_tap/packages.conf).
# useKPP is .FALSE. in every DINO namelist, and no run on scratch used KPP. Its one
# variant (scheme_tests/from_rest_viscRef_kppON) and input/data.kpp were deleted the
# same day; git history has both. To use KPP: uncomment it, restore data.kpp, set
# useKPP=.TRUE. in data.pkg, and comment out ivdc_kappa in data, since convective
# adjustment and KPP should not both act.
#kpp
#seaice
#salt_plume
#obcs
#down_slope

#debug
#autodiff
#cost
#ctrl
#grdchk
#ecco
#smooth
#profiles
rw
mdsio
# cd_code: the C-D scheme for the Coriolis terms. Compiled here from 2026-05-01 to
# 2026-09-12 only for two forward variants that set useCDscheme=.TRUE.; they were
# deleted on 2026-09-12 (git history has them), no run on scratch used the scheme,
# and the adjoint never compiled it. To use it: uncomment it and set
# useCDscheme=.TRUE. (PARM01) and tauCD (PARM03) in data. A namelist with the flag
# set stops at start-up if the package is not compiled (model/src/packages_check.F).
#cd_code
