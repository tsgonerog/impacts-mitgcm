#-- list of packages (or group of packages) to compile for this experiment:
oceanic
cd_code     # the C-D scheme for the Coriolis terms; compiled but not used here: input/data leaves useCDscheme off, only the adjoint's input_tap/data sets it (see README.md)
exf
seaice
salt_plume
ptracers
longstep
diagnostics
#timeave    <- pkg/timeave was removed upstream in c69m; use diagnostics instead
mnc
