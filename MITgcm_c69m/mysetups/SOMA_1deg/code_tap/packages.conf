#-- list of packages (or group of packages) to compile for this experiment:
oceanic # this pkg_group will include the packages "gfd, gmredi, kpp" (see MITgcm/pkg/pkg_groups)
cd_code # the C-D scheme for the Coriolis terms; used, since input_tap/data sets useCDscheme=.TRUE. (the forward input/data does not; see README.md)
exf     # external forcing package for applying time-varying surface fluxes (wind stress, heat, moisture, radiation, pressure) with optional bulk-formula physics.
#seaice
#salt_plume
#ptracers
longstep
diagnostics
#timeave
#mnc # dumps output in netCDF format
tapenade
adjoint # this pkg_group will include the packages "autodiff, ctrl, cost, grdchk"
